Принципы работы поискового движка следующие:
1. В конфигурационном файле перед запуском приложения задаются названия
файлов, по которым движок будет осуществлять поиск. --> config.json
2. Поисковый движок самостоятельно обходить все файлы и
индексирует их так, чтобы потом по любому поисковому запросу находить наиболее
релевантные документы.
3. Пользователь задаёт запрос через JSON-файл requests.json. Запрос — это
набор слов, по которым нужно найти документы.
    #requests состоит из списка запросов, которые необходимо обработать
     поисковым движком. Поле содержит не более 1000 запросов, каждый из
     которых включает от одного до десяти слов
    #<содержимое запроса>(“some words”) — поисковый запрос, набор слов,
     разделённых одним или несколькими пробелами. По ним необходимо
     осуществить поиск. Все слова состоят из строчных латинских букв.
    #max_responses максимальное кол-во ответов на запрос.
4. Запрос трансформируется в список слов.
5. В индексе ищутся те документы, на которых встречаются все эти слова.
6. Результаты поиска ранжируются, сортируются и отдаётся пользователю,
максимальное количество возможных документов в ответе задаётся в
конфигурационном файле. --> config.json
7. В конце программа формирует файл answers.json, в который записывает
результаты поиска.
# работа с json осуществляется через библиотерку nlohmann_json

#######
проект search_engine содержит классы:
# ConverterJSON для работы с файлами формата JSON.
# InvertedIndex, который хранит и индексирует слова
# SearchServer, который позволяет определить наиболее релевантные,
  соответствующие поисковому запросу документы по прочитанным из
  файла requests.json поисковым запросам.

#######

# cmake_minimum_required (VERSION 3.8)
# CMAKE_CXX_STANDARD 20
# Поместить файлы для индесирования в каталог ./resourses
# печеслить пути к файлам в файле config.json
# печеслить запросы в файле request.json
